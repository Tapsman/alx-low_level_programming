#include <stdio.h>
#include "mai.h"
/**
 * _isdigit.c - Checks for digit from 0 to 9
 * @c: Function parameter
 * Return: 1 on success 0 on failure
 */
int _isdigit(int c)
{
	if (c >= 48 && C <= 57)
		return (1);
	else
		return (0);
}
